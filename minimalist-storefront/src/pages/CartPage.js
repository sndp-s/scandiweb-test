import React, { Component } from "react";

// components
import Title from "../components/Title";

class CartPage extends Component {
  render() {
    return (
      <main className="container">
        <Title text="CART" pageStyle="CartPage">CART</Title>
      </main>
    );
  }
}

export default CartPage;
